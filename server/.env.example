# Backend environment variables for Render deployment
# Copy to .env and fill with your secrets (do NOT commit .env)

# MongoDB connection string
MONGO_URI=mongodb+srv://harish7875343884_db_user:LAVANYA5115@conectocluster.mcv6llu.mongodb.net/campusDB?retryWrites=true&w=majority&appName=conectoCluster

# JWT secret for signing tokens
JWT_SECRET=supersecret123

# Email sending
# Option A: Resend (recommended for Render; avoids SMTP blocks)
# https://resend.com 
RESEND_API_KEY=re_XqwsReor_AMQ6rYsHJZQrDWe9SnKRFunY
# Optional custom from name/email (used for both Resend and SMTP if set)
EMAIL_FROM="Campus Conecto <no-reply@campusconecto.com>"
# Frontend base URL used to build password reset links
FRONTEND_URL=https://campus-conecto.vercel.app

# Option B: Gmail SMTP (fallback)
EMAIL_USER=campusconecto@gmail.com
EMAIL_PASS=jmcckkmtrwssftbk

# Optional: Custom SMTP provider instead of Gmail
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=

# Optional: override port
# PORT=5000
